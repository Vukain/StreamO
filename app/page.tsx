import styles from './page.module.sass';

const Home = () => {
  return (
    <main className={styles.main}>
      <p>Redirecting to /streamers for consistency...</p>
    </main>
  );
};

export default Home;
